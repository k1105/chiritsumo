<h2><i><span class="text-inversion-style"><b>みつけちゃった？</b></span></i></h2>
<p><b><span class="text-inversion-style">ここは山岸専用ページです。こういうページは普通山岸以外見えないようにするものなのですが、怠惰ゆえにロックをかけていませんでした。
<br>みつかっちゃったときに対応しようと思っていたので、みつけたあなたは山岸のtwitterにご一報ください。</b></span></p>

<%= form_for @image do |form| %>
  <%= render 'layouts/error_messages', model: @image %>
  <%= form.file_field :image %>
  <%= form.submit "投稿" %>
<% end %>

<%@images.each do |i| %>
  <p>ID: <%= i.id %> <%= image_tag i.image.url %> <%= link_to '削除', "/post_images/#{i.id}", method: :delete %></p>
<% end %>
